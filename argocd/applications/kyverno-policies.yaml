apiVersion: argoproj.io/v1alpha1
kind: Application
metadata: { name: securecart-policies, namespace: argocd }
spec:
  project: default
  source: { repoURL: https://github.com/REPLACE_ME/securecart-gitops.git, targetRevision: main, path: policies/cluster, directory: { recurse: true } }
  destination: { server: https://kubernetes.default.svc, namespace: kyverno }
  syncPolicy: { automated: { prune: true, selfHeal: true } }
