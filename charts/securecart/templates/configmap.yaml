apiVersion: v1
kind: ConfigMap
metadata: { name: securecart-config, namespace: securecart }
data:
  DB_URL: jdbc:postgresql://securecart-postgres:5432/{{ .Values.postgres.database }}
  DB_USERNAME: {{ .Values.postgres.username | quote }}
  APP_USER_USERNAME: student
  APP_ADMIN_USERNAME: admin
